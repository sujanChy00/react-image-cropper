import ImageCropperApp from "@/components/ImageCropper/ImageCropperApp";

const Index = () => {
  return (
    <main className="min-h-screen bg-secondary/50 p-4 flex flex-col items-center justify-center">
      <ImageCropperApp />
    </main>
  );
};

export default Index;
